{
 "goal": "Prove every feature standalone against real data per docs/standalone_proof_plan.md; Claude plans+gates+commits, codex gpt-5.5 codes one atomic step per invocation.",
 "north_star": "GROUND-TRUTH PARITY (Israel, 2026-07-08): standalones must be IDENTICAL to live \u2014 given raw Jetson captures, the harness (real modules, real fixtures) must reproduce the exact results the Jetson produced, verified by diffing against Jetson logs/db/artifacts. Every harness ends with a PARITY leg (see plan doc STANDING RULE); contract pins alone never close a harness; missing Jetson-side ground truth means adding observability + refetching, not skipping. Every codex prompt carries this; every gate enforces it.",
 "parallelism_rule": "STANDING (Israel, 2026-07-08): run at MAX parallelism constantly without losing quality \u2014 every disjoint-directory codex lane filled (server harness dirs, detection harness dirs, client e2e vs client src are all separate lanes; read-only spec rounds are free); the gate stays serial and strict (run suite, review diff, reject dishonest tests, atomic commits); convert every landed spec into an implementation lane immediately; prefetch long poles (deps, builds, fixtures) the moment they're foreseeable.",
 "resume_contract": "On wake: 0) re-read north_star AND parallelism_rule \u2014 reject any drift from ground-truth parity even if tests are green; keep all lanes full; 1) read this file + docs/standalone_proof_plan.md checklists; 2) `git log --oneline -15` to see what landed; 3) run the harness suites to confirm green; 4) for any in_flight codex step: check the tree for its files \u2014 if present and green, gate+commit+tick checklist; if absent, re-issue that step verbatim; 5) issue the next unchecked step (one per codex invocation, workspace-write); 6) update this file; 7) do NOT ask the user anything \u2014 only write a blocked entry on a genuinely unrecoverable blocker. Parallel lanes allowed across DISJOINT harness dirs only. Fixtures live under .jetson-snapshot/proof_fixtures/ (gitignored; push/ contains SECRETS, never commit or log bytes). Capture new Jetson fixtures opportunistically when it is reachable.",
 "codex_invocation": "ALWAYS via the stall guard (skill codex-stall-guard): .claude/scripts/codex-guard.sh <logfile> codex exec --sandbox workspace-write --model gpt-5.5 -C <repo> \"ATOMIC STEP <id> ONLY of harness #<n> (spec: <specdoc>; checklist: docs/standalone_proof_plan.md)... NOTHING ELSE... run pytest until green; print tail; no git commit.\" \u2014 never pipe codex stdout through tail (blinds liveness); read the logfile tail afterwards.",
 "harnesses": {
  "1_retention": {
   "status": "DONE",
   "tests": "server/tests/harness_retention (9)"
  },
  "2_push_gateway": {
   "status": "DONE except P14",
   "remaining": "P14 parity diff \u2014 needs accumulated production \"push fanout event=\" INFO lines (deployed; real events flowing since ~08:15 + device-validation runs); re-fetch snapshot (deploy/fetch-jetson-data.sh grabs docker app log now) then diff harness fanout decisions vs logged sent/filtered counts",
   "tests": "server/tests/harness_push_gateway (9 local + 3 live env-gated HOMECAM_LIVE_PUSH=1)"
  },
  "3_auth": {
   "status": "DONE",
   "tests": "server/tests/harness_auth (7) + client/e2e auth-session-lifecycle (9 browser, HOMECAM_RUN_REAL_BROWSER_AUTH=1)"
  },
  "4_snapshot_thumb": {
   "status": "DONE 15/15",
   "tests": "server/tests/harness_snapshots + detection/tests/harness_snapshots_worker + 3 client test files"
  },
  "5_webrtc_whep": {
   "status": "in_progress",
   "done": "W1-W6 live-verified (all 3 rungs real frames) + W9-W12 error harness (raw-ASGI stub; abort-on-supersede proven with+without SW)",
   "in_flight": null,
   "remaining": "W7 quality-switch live, W8 resume-coalescing live, W13-W16 attempt-ledger parity vs mediamtx logs, W17 gate",
   "findings": "Live-pill playing-vs-rVFC fixed+deployed (57b251b); abort-on-supersede fixed+deployed (8fadee0); starlette BaseHTTPMiddleware swallows http.disconnect (documented in whepErrorHarness stub)"
  },
  "6_sw_cache_update": {
   "status": "in_progress",
   "done": "H6.1-6.12 (nav-fallback fix 81b7944 deployed; H6.11-12 pinned as honest skips \u2014 no Playwright OS-click trigger, device-validated on real phone)",
   "in_flight": null,
   "remaining": "H6.13-6.14 parity vs production log window"
  },
  "7_eventbus_sqlite": {
   "status": "DONE 16/16 incl. full-night parity replay"
  },
  "8_face_recognition": {
   "status": "DONE for current ground truth (14/15)",
   "remaining": "R15 named parity \u2014 armed by the R14 inverted sentinel; fires when a refreshed snapshot has named person rows",
   "note": "venv recipe for face_recognition/dlib is in the plan doc (setuptools<81)"
  },
  "9_export_zip": {
   "status": "DONE 13/13 incl. measured-RSS OOM guard"
  },
  "10_multicam": {
   "status": "in_progress",
   "done": "M10.11-12 parity (fa66692)",
   "in_flight": "M10.2-4 local mediamtx + testsrc publish + registry (task bd49lchrf; needs a mediamtx binary \u2014 env HOMECAM_MEDIAMTX_BIN or PATH, skip otherwise)",
   "remaining": "M10.5-10 browser switcher/rungs/worker-id/persistence/filter/live-probe"
  },
  "11_rebuild_list": {
   "status": "pending",
   "note": "OTA + backup/restore: de-stub honestly BEFORE proving"
  }
 },
 "production": {
  "deploys_today": "server x2 (TTL fix, fanout INFO logging, WS accept-then-close), client x4 (A8 refresh-before-anon, rVFC pill, abort-on-supersede, SW navigation fallback)",
  "all_verified": "healthz + in-container grep + live browser suites"
 },
 "blocked": [],
 "last_update": "2026-07-08T13:00 local, ready-to-compact checkpoint; M10.2-4 (task bd49lchrf) still in flight",
 "device_leg_2026_07_08": "Phone validations DONE via adb 100.107.247.98:36413 (reconnect if offered again): subscribe flow, push tray display w/ icon under DND, tap deep-link to exact event, live view authed w/ honest pill. Synthetic events cleaned from prod (events DB is /app/secrets/events.db in-container). Phone sub now ACTIVE (9 devices). Pinch-zoom left for manual thumb test."
}