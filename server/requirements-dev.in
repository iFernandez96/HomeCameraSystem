# Direct development/test requirements. Runtime dependencies are compiled into
# this lock as well so one --require-hashes install is sufficient in CI.
-r requirements.in

pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
