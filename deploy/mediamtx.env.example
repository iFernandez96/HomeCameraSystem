# Copy to /etc/homecam/mediamtx.env only after speaker hardware is installed.
# Then create the root-owned marker named below and restart mediamtx.service.
HOMECAM_SPEAKER_ENABLE_FILE=/etc/homecam/speaker-enabled
HOMECAM_SPEAKER_DEVICE=plughw:CARD=Device,DEV=0
HOMECAM_SPEAKER_VOLUME=1.0
