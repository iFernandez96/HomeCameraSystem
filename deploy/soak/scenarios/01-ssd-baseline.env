# Scenario 01 — current production detector.
#
# This MUST match what the deployed worker uses today. If you change
# DETECT_MODEL elsewhere, mirror it here so this baseline keeps its
# meaning across iterations.
DETECT_MODEL=ssd-mobilenet-v2
DETECT_ACTIVE_FPS=5.0
DETECT_IDLE_FPS=1.0
